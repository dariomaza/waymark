node_modules/
dist/
build/
.turbo/
.env
.env.*
!.env.example
*.db
*.db-journal
*.sqlite
uploads/
# Photo files. `WAYMARK_PHOTO_ROOT` defaults in here; production mounts a volume.
data/
.DS_Store
.expo/
# Generated by `expo prebuild` from the Expo config. The native projects are
# output, not source: regenerating them is how a config plugin takes effect.
apps/mobile/android/
apps/mobile/ios/
.atl/
.codegraph/

# Operator-specific deploy target. The committed example is deploy.env.example.
scripts/deploy.env

# Personal, per-machine Claude Code files. Project-wide ones (CLAUDE.md,
# .claude/rules/) ARE committed.
CLAUDE.local.md
.claude/settings.local.json
